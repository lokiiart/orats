<%= form_for(virtual_order) do |f| %>
  <% if virtual_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(virtual_order.errors.count, "error") %> prohibited this virtual_order from being saved:</h2>

      <ul>
      <% virtual_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "客户姓名" %>
    <%= f.text_field :name %>
  </div>

  <div class="form-group">
    <%= f.label "联系方式" %>
    <%= f.text_field :phone %>
  </div>

  <div class="form-group">
    <%= f.label "订购数量" %>
    <%= f.text_field :product_count %>
  </div>

  <div class="form-group">
    <%= f.label "付款方式" %>
    <%= f.text_field :payment %>
  </div>

  <div class="form-group">
    <%= f.label "发货地址" %>
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit "立即抢购", class: "btn btn-default" %>
  </div>
    <br>
<% end %>
